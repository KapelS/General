<!DOCTYPE html>
<html>
  <head>
    <title>Twitterblog</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>


    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
  <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
  <div>
    <h4 class="font-weight-bold mb-0">
      <%= image_pack_tag 'twitter-logo.png', size: '35x30', alt: 'Logo', :class => "mb-2"  %>
      <%= link_to "Twitter Blog", home_path, :class => "blog-title" %>
    </h4>
  </div>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse d-flex justify-content-end" id="navbarNavDropdown">
    <ul class="navbar-nav">
      <li class="nav-item active">
        <span class="nav-link font-weight-bolder"> <%= link_to "Главная страница", home_path, :class => "nav-link" %> <span class="sr-only">(current)</span></span>
      </li>
      <li class="nav-item">
        <span class="nav-link font-weight-bolder" href="#"><%= link_to "О нас", about_path, :class => "nav-link" %></span>
      </li>
       <li class="nav-item">
        <span class="nav-link font-weight-bolder" href="#"><%= link_to "Добавить пост", new_post_path, :class => "nav-link" %></span>
      </li> 

<ul class="navbar-nav">
 <% if user_signed_in? %>
        <li class="nav-item active">
          <span class="nav-link font-weight-bolder"> <%= link_to "Выход", destroy_user_session_path, method: :delete, class: "nav-link" %> <span class="sr-only">(current)</span></span>
        </li>
  <% else %>
        <li class="nav-item active">
          <span class="nav-link font-weight-bolder"> <%= link_to "Вход", new_user_session_path, class: "nav-link" %> <span class="sr-only">(current)</span></span>
        </li>
        <li class="nav-item active">
          <span class="nav-link font-weight-bolder"> <%= link_to "Регистрация", new_user_registration_path, class: "nav-link" %> <span class="sr-only">(current)</span></span>
        </li>
      <% end %>
    </ul>
  </div>
  

  <div class= "col-md-12">
    <% flash.each do |name, message| %>
      <div class="alert alert-<%= name %> <% bootstrap_class_for(name) %>">
        <button class="close" data-dismiss="alert">&times;</button>
        <%= message %>
      </div>
      <% end %>
  </div>


</nav>

<div class="container">
    <%= yield %>
  </div>
  </body>
</html>
